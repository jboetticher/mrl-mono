{
	"name": "mrl-indexer",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"deploy": "npx wrangler deploy",
		"dev": "npx wrangler dev --local --test-scheduled"
	},
	"dependencies": {
		"@cloudflare/wasm-coredump": "1.0.3"
	},
	"devDependencies": {
		"miniflare": "^2.14.1",
		"wrangler": "^2.2.4"
	}
}
